# Use hoisted linker (flatter node_modules like npm/yarn)
node-linker=hoisted

# Ensure symlinks work in WSL/Windows
symlinks=true

# Save exact versions instead of ranges (optional, I prefer this for infra code)
save-exact=true

# Auto prune store on install to avoid old junk
prefer-offline=false
